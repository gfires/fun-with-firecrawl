# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.tsbuildinfo

# caches — large, local-only
data/scrape-cache.json
data/search-cache.json

# trace output — large JSON files from orchestrated runs
trace-output/

# env — never commit real keys
.env
.env.local
.env*.local
.ittybitty/agents/
.ittybitty/archive/
compare-output/
.gstack/
