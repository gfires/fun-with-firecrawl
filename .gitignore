# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.tsbuildinfo

# caches — large, local-only
data/scrape-cache.json
data/search-cache.json

# env — never commit real keys
.env
.env.local
.env*.local
.ittybitty/agents/
